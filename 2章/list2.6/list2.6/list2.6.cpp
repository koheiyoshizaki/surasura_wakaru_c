#include<stdio.h>

int main(void){

	//printf("テストの結果は%u点でした。", -10);
	//printf("テストの結果は%d点でした。", 10.0);
	//printf("テストの結果は%f点でした。", 10);
	//printf("テストの結果は%d点でした。", 'A');
	//printf("テストの結果は%c点でした。", 65);
	printf("テストの結果は%cでした。\n点数でいうと%d点です。", 'A', 0);

	return 0;
}